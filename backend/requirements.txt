# ===========================================
# Spiegel Backend Dependencies
# ===========================================
# Python 3.11+ required
# Install: pip install -r requirements.txt
# ===========================================

# ============= Core framework =============
flask>=3.0.0
flask-cors>=6.0.0

# ============= LLM =============
# OpenAI SDK (all LLM calls use the OpenAI format)
openai>=1.0.0

# ============= Vector index =============
# Semantic search over the posts/comments the simulated audience wrote.
# Runs embedded (local file storage) unless QDRANT_URL is set.
# The embedding model is not a Python dependency - it is served over HTTP by the
# self-hosted LM Studio box, see EMBEDDING_BASE_URL.
qdrant-client>=1.9.0

# ============= Zep Cloud =============
zep-cloud==3.25.0
httpx>=0.27.0

# ============= OASIS social media simulation =============
# OASIS social simulation framework
camel-oasis==0.2.5
camel-ai==0.2.78

# ============= Corpus: Threads source (optional) =============
# Only needed when config/corpus.yml enables the `threads` source, which renders
# threads.net in a headless browser because there is no public search API.
# Chromium is a further ~300 MB and is NOT installed by pip:
#   pip install playwright lxml && playwright install chromium
# Left commented so the default image stays small; the adapter reports itself
# unavailable rather than failing when these are absent.
# playwright>=1.44.0
# lxml>=5.0.0

# ============= File processing =============
PyMuPDF>=1.24.0
# Encoding detection (supports non-UTF-8 text files)
charset-normalizer>=3.0.0
chardet>=5.0.0

# ============= Utilities =============
# Environment variable loading
python-dotenv>=1.0.0
PyYAML>=6.0

# Data validation
pydantic>=2.0.0
